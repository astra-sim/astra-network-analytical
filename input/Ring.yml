# Network Configuration

# 1D basic topology (Ring)
topology: [ Ring ]  # Ring

# Ring with 16 NPUs
npus_count: [ 16 ]  # number of NPUs

# Bandwidth per each dimension
bandwidth: [ 50.0 ]  # GB/s

# Latency per each dimension
latency: [ 500.0 ]  # ns
