# Network Configuration

# 1D basic topology (Switch)
topology: [ Switch ]  # Switch

# Switch with 16 NPUs
npus_count: [ 16 ]  # number of NPUs

# Bandwidth per each dimension
bandwidth: [ 50.0 ]  # GB/s

# Latency per each dimension
latency: [ 500.0 ]  # ns
